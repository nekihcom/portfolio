/**
 * プロジェクトのテーマ設定
 * 
 * カラーパレット
 * - 差し色: teal
 *   - 用途: アクセントカラー、ボタン、リンク、ハイライト
 *   - バリエーション:
 *     - teal-600: メインの差し色
 *     - teal-700: ホバー時の色
 *     - text-teal-600: テキストの差し色
 * 
 * 使用ルール
 * 1. アクセントが必要な要素には必ずtealを使用する
 * 2. ボタンやリンクなどのインタラクティブな要素はteal-600を使用
 * 3. ホバー時はteal-700を使用
 * 4. テキストのアクセントにはtext-teal-600を使用
 */

export const theme = {
  colors: {
    accent: {
      main: 'teal-600',
      hover: 'teal-700',
      text: 'text-teal-600',
    },
  },
} 